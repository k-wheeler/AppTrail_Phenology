var identify_locations_File = require('users/kwheeler/AppTrail_Phenology:IdentifyLocations')

var MA_forest = identify_locations_File.identify_locations()

Map.addLayer(
  MA_forest.selfMask(),
  {palette: ['green']},
  'Deciduous + Mixed Forest'
);